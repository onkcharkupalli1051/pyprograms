class Node:
    def append(self,v):
        if self.isempty():
            self.value = v
        elif self.next = None:
            newnode = Node(v)
            self.next = newnode
        else:
            (self.next).append(v)
        return ()

    